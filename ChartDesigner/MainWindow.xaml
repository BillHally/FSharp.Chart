<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oxyplot="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
    mc:Ignorable="d"
	Title="Chart designer"
    MinWidth="200"
    MinHeight="100"
    >
    <Grid
        >
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Viewbox
            Grid.Row="0"
            Grid.Column="0"
            Stretch="Uniform"
            StretchDirection="Both"
            >
            <oxyplot:PlotView
                MinHeight="{Binding ElementName=VerticalSlider, Path=Value}"
                MinWidth="{Binding ElementName=HorizontalSlider, Path=Value}"
                Model="{Binding Model}"
                />
        </Viewbox>

        <Slider
            Grid.Row="1"
            Grid.Column="0"
            Minimum="100"
            Value="800"
            Maximum="1000"
            Orientation="Horizontal"
            x:Name="HorizontalSlider"
            />

        <Slider
            Grid.Row="0"
            Grid.Column="1"
            Minimum="100"
            Value="600"
            Maximum="1000"
            Orientation="Vertical"
            x:Name="VerticalSlider"

            />
    </Grid>
</Window>
